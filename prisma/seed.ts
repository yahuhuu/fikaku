const defaultExpenseCategories = [
  "Food",
  "Transport",
  "Bills",
  "Entertainment",
  "Health",
  "Education",
  "Other",
];

const defaultIncomeCategories = ["Salary", "Freelance", "Investment", "Other"];

console.info("Default expense categories:", defaultExpenseCategories.join(", "));
console.info("Default income categories:", defaultIncomeCategories.join(", "));
console.info("Seed users after auth is connected to your VPS database.");
